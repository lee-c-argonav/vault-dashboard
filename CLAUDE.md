# CLAUDE.md

Claude Code reads `CLAUDE.md`; Kimi Code and most other agents read `AGENTS.md`.
This repo's instructions live in `AGENTS.md` so both agents get the same file,
with no second copy to keep in sync.

Read it now, in full. Claude Code resolves the import below automatically; other
agents must open the file with a file read.

@AGENTS.md
