{
  "name": "vault-hud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only instrument panel for an Obsidian vault. Zero dependencies.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
